import path from 'path';

export const ROOT_PATH = path.join(__dirname, '..');

export const DOCKER_COMPOSE_DEV_FILE = 'docker-compose.dev.yml';
export const DOCKER_COMPOSE_FAKE_PROD_FILE = 'docker-compose.yml';

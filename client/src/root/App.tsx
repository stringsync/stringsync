import * as React from 'react';
import { Button } from 'antd';

const App: React.SFC = () => (
  <div>
    App, this should build 1:14 PM
    <Button type="dashed" >Papa Bless</Button>
  </div>
);

export default App;

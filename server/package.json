{
  "name": "string-sync-server",
  "version": "1.0.0",
  "description": "Serve requests from stringsync.com.",
  "repository": "https://github.com/jaredjj3/string-sync",
  "author": "Jared Johnson",
  "main": "server.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "ensure-server-file": "mkdir -p build && touch ./build/server.js",
    "start": "yarn ensure-server-file && node ./build/server.js",
    "dev": "yarn ensure-server-file && nodemon --delay 500ms --watch build ./build/server.js",
    "watch": "tsc -w",
    "compile": "tsc -p .",
    "check": "gts check",
    "clean": "gts clean",
    "fix": "gts fix"
  },
  "dependencies": {
    "express": "4.17.1",
    "graphql": "^14.4.2",
    "prisma-client-lib": "1.34.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/graphql": "^14.2.2",
    "gts": "^1.0.0",
    "nodemon": "^1.19.1",
    "prisma": "^1.34.0",
    "ts-node": "^8.3.0",
    "typescript": "~3.4.0"
  }
}

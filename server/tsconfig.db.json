{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // should only be run by tsc, not webpack
    // see sql script for usage
    "outDir": "tsc",
    "allowJs": true,
    "isolatedModules": true
  },
  "include": ["src/db/**/*.ts"],
  "exclude": ["node_modules"]
}

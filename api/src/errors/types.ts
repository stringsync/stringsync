export enum HttpStatus {
  OK = 200,
  INTERNAL_SERVER_ERROR = 500,
}

export enum ErrorCode {
  UNKNOWN,
  FORBIDDEN,
  CONFLICT,
  INVALID,
  BAD_REQUEST,
  NOT_FOUND,
  INTERNAL,
  NOT_IMPLEMENTED,
}

import _styled from 'styled-components';
import { Styled } from './types';

// The purpose of this utility is to provide type information
// on the styled singleton.
export const styled: Styled = _styled;

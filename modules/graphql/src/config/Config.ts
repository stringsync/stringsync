export interface Config {
  NODE_ENV: string;
  PORT: string;
  WEB_URI: string;
  DB_NAME: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_HOST: string;
  DB_PORT: string;
  REDIS_HOST: string;
  REDIS_PORT: string;
  ROLE: string;
  SESSION_SECRET: string;
}
